---
import { Newsletter } from '@/components/common/Newsletter';
import { SocialIcons } from '@/components/common/SocialIcons';
import type {
  FooterDataProps,
  NewsletterDataProps,
} from '@/types/common_types';

interface Props {
  lg_screen_width: string;
  default_screen_width: string;
  footerData: FooterDataProps;
  newsletterData: NewsletterDataProps;
  locale?: string;
}

const {
  lg_screen_width,
  default_screen_width,
  footerData,
  newsletterData,
  locale,
} = Astro.props;
---

<div
  class="w-screen pt-10 pb-[58px] bg-black flex flex-col justify-center items-center"
>
  <div class={`${lg_screen_width} ${default_screen_width}`}>
    <Newsletter locale={locale ?? 'en'} newsletterData={newsletterData} client:visible/>
    <div class="mt-10 flex justify-end">
      <SocialIcons />
    </div>
    <p
      class="text-white mt-10"
      set:html={footerData.copyright}
    />
  </div>
</div>
